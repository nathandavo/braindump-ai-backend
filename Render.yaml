services:
  - type: web
    name: footydarts-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: API_FOOTBALL_KEY
        sync: false   # you’ll fill this in next screen
